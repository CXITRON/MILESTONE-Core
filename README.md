# MILESTONE Core — MILESTONE D1

ESP32-S3 Zero와 SH1107 128×128 OLED용 통합 펌웨어입니다. OLED 주소는 부팅할 때 `0x3C`, `0x3D` 순서로 자동 탐색하며, 배선은 다음과 같습니다.

| OLED | ESP32-S3 Zero |
|---|---|
| VCC | 3V3 |
| GND | GND |
| SDA | GPIO 8 |
| SCL | GPIO 9 |

## 1. Arduino IDE 준비 (Arch Linux)

현재 확인된 환경:

- Arduino IDE
- esp32 by Espressif Systems `3.3.11`
- 보드: `Waveshare ESP32-S3-Zero`
- 포트: `/dev/ttyACM0`
- `Tools > USB CDC On Boot > Enabled`
- `Tools > PSRAM > Enabled` (공식 Waveshare 보드 프로필 사용)
- `Tools > Partition Scheme > Minimal SPIFFS (1.9MB APP with OTA/190KB SPIFFS)`

1.5.0부터는 인터넷 업데이트를 위해 OTA 앱 슬롯 2개가 반드시 필요합니다. 위 파티션 항목의 문구는 ESP32 보드 패키지 버전에 따라 조금 다를 수 있으므로, 반드시 **OTA 포함**, 앱 슬롯 약 **1.9MB**인 항목을 선택합니다. `Huge APP`처럼 OTA 슬롯이 하나뿐인 구성은 사용할 수 없습니다.

Waveshare ESP32-S3-Zero의 ESP32-S3FH4R2에는 2MB QSPI PSRAM이 내장되어 있습니다. 현재 화면·OTA 버퍼와 주요 객체는 여전히 내부 RAM을 사용하지만 1.5.7 릴리스부터 공식 Waveshare 보드 프로필의 `PSRAM > Enabled` 설정으로 빌드합니다. 향후 카메라 프레임, 대형 이미지·오디오 버퍼, 머신러닝 모델 같은 대용량 기능을 추가할 수 있으며, 다른 ESP32-S3 프로필에서 OPI PSRAM을 강제로 선택하지 마십시오.

Arduino IDE의 `Tools > Manage Libraries…`에서 다음 라이브러리를 설치합니다.

- `U8g2` by oliver
- `Adafruit NeoPixel` by Adafruit

`HTTPClient`, `NetworkClientSecure`, `Update`, `mbedtls`는 ESP32 보드 패키지에 포함되므로 별도 라이브러리를 설치하지 않습니다.

이전 OLED 확인에 사용한 Adafruit GFX·SH110X 라이브러리는 이 프로젝트에서 필요하지 않습니다.

## 2. 펌웨어 열기와 업로드

1. 압축을 풉니다.
2. `MILESTONE_Core/MILESTONE_Core.ino`를 Arduino IDE로 엽니다.
3. 위 보드·포트·USB CDC 설정을 다시 확인합니다.
4. 먼저 왼쪽 위의 **검증(✓)** 버튼을 누릅니다.
5. 오류가 없으면 **업로드(→)** 버튼을 누릅니다.
6. 출력 끝에 `Hard resetting via RTS pin...`이 나타나면 업로드 완료입니다. 멈춘 것이 아닙니다.
7. OLED가 바뀌지 않으면 보드의 `RESET` 버튼을 한 번 눌렀다 놓습니다.

1.7.0부터 `MILESTONE_Core.ino`는 공통 선언과 Arduino 진입점만 유지하고, 구현부를 같은 translation unit에 포함되는 `CoreConfig.inc`, `CoreDiagnostics.inc`, `CoreRollback.inc`, `CoreDisplay.inc`, `CoreNetwork.inc`, `CoreUpdate.inc`, `CorePortal.inc`, `CoreRuntime.inc`로 기계적으로 분리합니다. Arduino IDE에서는 이전과 동일하게 `MILESTONE_Core.ino`만 열면 됩니다.

1.3.1에서 1.5.0으로 넘어갈 때는 파티션 테이블도 OTA용으로 바뀌어야 하므로 **Arduino IDE의 일반 업로드**를 사용해야 합니다. 앱 BIN 하나만 `0x10000` 주소에 기록하는 방식으로 최초 설치하지 마십시오. 1.5.0이 정상 설치된 뒤부터 Release의 `MILESTONE_Core.bin`만으로 인터넷 업데이트할 수 있습니다.

업로드가 포트 대기 상태에서 실패할 때만 다음 순서로 수동 다운로드 모드에 진입합니다.

1. `BOOT`을 누른 채 유지합니다.
2. `RESET`을 한 번 눌렀다 놓습니다.
3. `BOOT`을 놓습니다.
4. `/dev/ttyACM0`을 다시 선택하고 업로드합니다.
5. 완료 뒤 `RESET`을 한 번 누릅니다.

## 3. 최초 설정

저장된 Wi-Fi가 없으면 OLED에 아래 정보가 나타납니다.

- Wi-Fi 이름: `MILESTONE-D1-SETUP`
- 비밀번호: OLED에 표시되는 매번 새로 생성된 8자리 값
- 설정 주소: `http://192.168.4.1`

휴대폰이나 PC에서 해당 Wi-Fi에 접속합니다. “인터넷 없음” 경고는 정상입니다. 자동 설정 창이 뜨지 않으면 브라우저에서 `http://192.168.4.1`을 직접 엽니다.

1. **주변 Wi-Fi 검색**을 누르고 사용할 **2.4GHz Wi-Fi**를 선택합니다.
2. 일반 가정용 Wi-Fi는 `Personal / Open`, 학교·기관의 WPA2-Enterprise 계정형 Wi-Fi는 `WPA2-Enterprise (PEAP)`를 선택합니다. 지원 가능한 Enterprise AP가 감지되면 자동 선택됩니다. WPA3-Enterprise/CA 필수 AP는 검색 결과에서 지원 불가로 표시됩니다.
3. Personal은 비밀번호를 입력하고, Enterprise는 사용자명·비밀번호와 필요 시 identity를 입력합니다.
4. **Wi-Fi 시험 후 확정 저장**을 누릅니다.
5. ESP32가 설정 Wi-Fi를 유지한 상태로 Wi-Fi와 NTP를 시험합니다.
6. 성공해야만 새 네트워크 자격 증명이 저장되며, 약 3초 뒤 설정 Wi-Fi가 닫힙니다.

화면 내용과 모드는 별도의 **화면·시간 설정 저장** 버튼으로 저장합니다. Wi-Fi 비밀번호와 Enterprise 사용자명·identity는 조회 API나 OLED에 다시 표시되지 않습니다. 같은 저장 네트워크를 다시 시험할 때 자격 증명 입력칸을 비워 두면 NVS에 저장된 값을 내부적으로 재사용합니다.

연결에 성공한 Wi-Fi는 Personal/Enterprise 구분과 함께 최근 성공 순으로 최대 8개까지 기억합니다. 부팅할 때 가장 최근에 성공한 네트워크를 먼저 시도하고, 실패하면 주변에서 검색되는 저장 네트워크를 신호가 강한 순서로 시도합니다. 목록이 가득 찬 상태에서 새로운 Wi-Fi를 저장하면 가장 오래 사용되지 않은 항목이 제거됩니다. 저장된 SSID를 같은 보안 방식으로 다시 선택하고 자격 증명 입력칸을 모두 비워 두면 기존 값을 재사용하며, 포털에서 각 항목을 개별 삭제할 수 있습니다.

### Enterprise Wi-Fi 지원

현재 펌웨어는 ESP32-S3의 802.1X Enterprise 기능을 사용해 **WPA2-Enterprise PEAP 사용자명/비밀번호 인증**을 지원합니다. identity는 선택 사항이며 비워 두면 사용자명을 outer identity로 사용합니다. 일반적인 학교 Wi-Fi처럼 SSID 접속 단계에서 계정 ID와 비밀번호를 요구하는 환경을 대상으로 합니다.

현재 설정 포털에서는 인증서 기반 EAP-TLS 및 사용자 지정 CA 인증서를 받지 않습니다. 따라서 학교에서 CA 인증서 검증이나 EAP-TLS를 강제하는 경우에는 연결할 수 없습니다. WPA3-Enterprise는 현재 정식 지원 범위에서 제외하며, 스캔에서 감지되면 CA 인증 지원이 필요하다는 안내를 표시합니다.

1.8.3 이하에서 저장한 기존 Wi-Fi와 일반 설정은 현재 설정 스키마 9로 자동 인식됩니다. 기존 `wifi_ssidN`과 `wifi_passN` 영역은 Wi-Fi Bank A로 그대로 유지하고, Wi-Fi 목록을 실제로 변경할 때만 비활성 Bank에 새 목록을 완전히 기록·검증한 뒤 활성 Bank를 전환합니다. 따라서 기존 Personal/Enterprise Wi-Fi는 다시 입력할 필요가 없습니다. **표시 설정 기본값 복원** 역시 Personal/Enterprise를 포함한 저장 Wi-Fi 전체를 보존하며, **공장 초기화**만 모든 Wi-Fi 자격 증명과 커스텀 미디어를 삭제합니다.

## 4. 설정 초기화와 공장 초기화

설정 포털의 두 초기화 기능은 서로 다릅니다.

| 기능 | 화면·시간·LED 설정 | 저장된 Wi-Fi | 진단 이력 | 커스텀 미디어 | 재부팅 |
|---|---|---|---|---|---|
| 표시 설정 기본값 복원 | 기본값으로 복원 | 유지 | 유지 | 유지 | 하지 않음 |
| 공장 초기화 | 기본값으로 복원 | 모두 삭제 | 모두 삭제 | 모두 삭제 | 실행 |

표시 설정 기본값 복원은 마지막 시간 동기화 시각과 오프라인용 디데이 계산값도 유지합니다. 공장 초기화는 ESP32 Wi-Fi 드라이버에 남을 수 있는 자격 증명까지 삭제합니다.

## 5. BOOT 버튼 조작

기능은 버튼을 **놓는 순간** 확정됩니다.

| 누른 시간 | 동작 |
|---|---|
| 50ms 미만 | 무시 |
| 50ms 이상 1초 미만 | 다음 화면, 현재 화면 저장 |
| 1초 이상 3초 미만 | 아무 동작 없음 |
| 3초 이상 8초 미만 | 설정 Wi-Fi 열기 |
| 8초 이상 | 초기화 확인 화면 |

초기화 확인 화면에서는 5초 안에 다시 `BOOT`을 누르기 시작한 뒤 3초 이상 누르고 놓아야 실제 초기화됩니다. 두 번째 누르기를 제한시간 안에 시작했다면 3초를 채우는 동안 확인 시간이 연장됩니다. 짧게 누르거나 누르지 않은 채 5초가 지나면 취소됩니다.

자동 순환으로 바뀐 화면은 저장하지 않습니다. 짧은 BOOT 입력으로 수동 전환한 화면만 저장되며, 재부팅하면 마지막으로 수동 저장한 화면에서 시작합니다.

## 6. 기기 세부정보 화면

일곱 번째 화면은 5초마다 다음 페이지로 자동 전환하며, 다른 화면으로 나갔다가 다시 들어오면 첫 페이지부터 시작합니다. BOOT 버튼의 2초 수동 페이지 전환 기능은 안정성을 위해 제거했습니다. 시스템, 메모리, 저장공간, 네트워크, 시간·업데이트 상태와 펌웨어 버전, 64비트 가동 시간, 재부팅 원인, 칩 모델·리비전·코어 수, CPU 주파수와 온도, 힙·작업 스택, 플래시·앱·OTA 공간, SSID·RSSI·채널·IP·게이트웨이·MAC, NTP 및 업데이트 확인 정보를 확인할 수 있습니다.

기존 설정은 현재 스키마 9로 자동 변환됩니다. 1.5.x의 저장된 Personal Wi-Fi, 1.6.0의 Enterprise Wi-Fi, 기존 화면·순환 설정은 유지됩니다. 커스텀 미디어 화면은 기존 순환 순서 뒤에 추가되지만 기본적으로 순환에 포함되지 않습니다. Wi-Fi 목록 저장은 A/B Bank 전환 방식으로 보강되어 목록 재정렬 중 전원 차단에도 직전 완전한 목록을 유지하도록 설계했습니다.

## 7. 진단 및 상태 이력

1.8.2부터 설정 포털에 **진단** 영역이 추가됩니다. 기기는 최근 16개의 중요한 상태 이벤트만 별도 `milestone_diag` NVS namespace에 고정 크기 ring buffer로 저장합니다. 설정 스키마는 계속 8이며, 일반 표시 설정이나 Wi-Fi 목록 migration에는 영향을 주지 않습니다.

기록 대상은 부팅·부팅 정상 확인, Wi-Fi 연결/시간 초과/연결 해제, NTP 성공/실패, 업데이트 확인 실패, OTA 시작/실패/재부팅 준비, 롤백 보호·candidate 부팅·롤백 실행·확정, 온도 경고/감속/안전모드/센서 오류, 미디어 저장소·검증·재생 실패입니다. 같은 이벤트와 detail이 짧은 시간 안에 반복되면 중복 기록을 억제해 Flash write를 줄입니다. 매 loop, RSSI 변화, 일반 화면 전환은 저장하지 않습니다.

Wi-Fi 실패 이력에는 가능한 경우 ESP32 드라이버의 disconnect reason code도 남깁니다. NTP가 아직 성공하지 않은 부팅 초반 이벤트는 절대 시각 대신 uptime을 보존하고, 시간 동기화 이후 이벤트는 KST 시각과 uptime을 함께 확인할 수 있습니다.

포털에서 확인할 수 있는 항목은 다음과 같습니다.

- 현재 부팅의 reset reason과 약 10초 정상 실행 확인 여부
- 현재 부팅 세션의 최고 칩 온도
- 최근 OTA 결과와 롤백 결과
- 가장 최근 오류 이벤트
- 최신순 최근 16개 진단 이벤트
- **진단 정보 복사** 버튼으로 상태·이력을 한 번에 클립보드 복사
- **진단 이력 지우기** 버튼으로 진단 기록만 삭제

진단 이력 지우기는 화면 설정·저장 Wi-Fi·OTA rollback state를 변경하지 않습니다. 반면 **공장 초기화**는 일반 설정과 Wi-Fi뿐 아니라 진단 이력도 함께 삭제합니다.

### 7.1 커스텀 미디어

v1.9.0부터 설정 포털의 **커스텀 미디어** 카드에서 사진, GIF, 로컬 영상을 128×128 흑백 화면으로 변환해 **기기에 저장**할 수 있습니다. 원본 파일은 휴대폰·PC 브라우저 안에서만 처리되며, 기기에는 오디오와 원본 코덱 없이 1비트 `MSM1` 변환본만 전송됩니다. 이 절의 제한은 저장형 MSM1 미디어에만 적용됩니다. YouTube 페이지 URL과 오디오는 저장형 미디어 입력으로 지원하지 않으며, 긴 영상의 실시간 재생은 아래 **실시간 스트리밍** 기능을 사용합니다.

1. 파일을 선택하고 화면 채우기/전체 보기, 밝기·대비·반전, 투명 여백과 디더링을 정합니다.
2. GIF·영상은 5/8/10fps, 반복 여부를 정합니다. 영상은 시작 지점도 지정할 수 있으며 고정 40초 제한 없이 최대 1024프레임과 변환 결과 용량 한도 안에서 처리합니다.
3. **변환 및 미리보기**로 실제 OLED 픽셀과 정확한 결과 용량을 확인합니다.
4. 이름과 항목 표시시간을 정한 뒤 **기기에 업로드**합니다.
5. 목록에서 사용 여부, 이름, 표시시간, 순서와 삭제를 관리하고, **표시 모드**에서 커스텀 미디어 직접 모드 또는 순환 bit 7을 선택합니다.

한 프레임의 원시 크기는 2,048바이트이며 저장형 MSM1은 최대 1024프레임을 허용합니다. 전체 사용자 미디어 한도는 실제 파일시스템 크기를 읽어 `min(160KiB, 전체-16KiB)`로 결정하고 최대 항목은 64개입니다. 따라서 이론상 프레임 상한은 5fps 204.8초, 8fps 128초, 10fps 102.4초이지만 실제로는 XOR-RLE 압축 결과와 160KiB 저장 한도에 먼저 도달할 수 있습니다. 변환 결과가 한도를 넘으면 FPS나 영상 범위를 줄여야 합니다.

업로드는 임시 파일 수신, 크기·CRC32·프레임 검증, 최종 rename, A/B 목록 확정 순서로 처리합니다. 저장소 마운트 실패나 손상 미디어는 시간·Wi-Fi·OTA·부팅 검증을 막지 않습니다. 자동 포맷은 하지 않으며, 모든 미디어 삭제를 감수할 수 있을 때만 `REPAIR` 확인이 필요한 **저장소 복구**를 사용합니다. OTA와 미디어 업로드는 동시에 시작되지 않습니다.

v1.9.1에서는 모바일 브라우저의 사진/GIF 선택과 동영상 선택을 분리했습니다. 동영상 제공자가 MIME 형식을 비워 전달해도 MP4·M4V·MOV 확장자로 판정하며, iOS/WebKit에서 영상 정보나 프레임 준비가 끝나지 않을 때는 무기한 멈추지 않고 구체적인 오류를 표시합니다. 실제 디코딩 가능 여부는 브라우저와 원본 코덱에 따라 달라질 수 있으며, 호환성이 가장 높은 형식은 H.264 비디오가 포함된 MP4입니다.

v1.9.2에서는 Safari가 동영상 선택 후 파일을 반환하지 않는 경우를 피하기 위해 파일 입력의 네이티브 형식 필터를 제거했습니다. 선택된 파일은 브라우저에서 MIME과 확장자를 함께 검사하며, iPad에서는 사진 또는 파일 앱의 항목을 설정 페이지의 끌어놓기 영역으로 직접 전달할 수도 있습니다.

v1.9.3에서는 파일 선택을 하나의 성공/실패로 취급하지 않고 선택기 실행, 운영체제·파일 제공자의 원본 준비, 브라우저 `File` 전달, 데이터 접근 확인, 영상 메타데이터 분석, 프레임 변환 단계로 구분해 표시합니다. 사진 앱용 동영상 선택과 형식 제한이 없는 파일 앱 선택을 별도로 제공하며, `input`·`change` 이벤트와 입력 폴링을 함께 사용합니다. 파일 전달이 늦으면 경과 시간을 계속 표시하고 180초 동안 전달되지 않으면 선택/제공자 단계의 실패로 판정합니다. **파일 선택 진단 보기**에서는 브라우저 정보와 실제 발생한 이벤트를 복사할 수 있습니다.

v1.9.4에서는 실제 영상 선택 뒤 WebKit이 `cancel`과 `files=0`을 반환한 진단 결과를 반영했습니다. 이 이벤트는 사용자가 취소 버튼을 눌렀다는 의미로만 해석할 수 없으므로 즉시 대기를 종료하지 않고, 지연된 파일 제공자 전달을 180초까지 계속 감시합니다. 또한 네이티브 선택기를 여는 첫 클릭에서 비어 있는 파일 입력값을 다시 초기화하지 않아 WebKit 활성화 도중 입력 상태를 건드리지 않습니다.

### 7.2 실시간 스트리밍

v1.10.5부터 **실시간 스트리밍**은 저장형 미디어와 분리된 `/stream` 전용 페이지와 독립 `STREAM_MODE`에서 동작합니다. 휴대폰·PC 브라우저가 영상을 먼저 128×128 1비트 프레임으로 전체 선변환한 뒤 정속으로 전송하므로, ESP32는 원본 영상 디코딩이나 전체 영상 저장을 하지 않습니다.

- 입력: 로컬 영상 파일 또는 브라우저가 직접 읽을 수 있고 CORS가 허용된 직접 영상 URL
- FPS: 5 / 10 / 15 / 18 / 20fps, 24fps Experimental
- 전송: `application/octet-stream` raw binary + 작은 binary ACK. v1.10.6부터 session/sequence/frame-count 메타데이터는 raw-body 콜백에서도 확실히 읽히도록 명시적으로 수집한 `X-MILESTONE-*` 요청 헤더로 전달
- 버퍼: PSRAM 32프레임 링버퍼, 초기 선버퍼 후 장치 처리량에 맞춰 프레임 드롭
- 저장 한도: MSM1의 1024프레임·160KiB 제한을 적용하지 않으며 스트림 프레임을 LittleFS/NVS에 쓰지 않음
- 실행 격리: 스트리밍 중 일반 NTP·OTA·진단·화면 순환·저장형 미디어·일반 LED 처리와 불필요한 포털 API를 차단하고 수신·버퍼·OLED·BOOT·온도/자원 보호에 집중
- 열 정책: 75°C 경고, 80°C에서 스트림 중단, 85°C 이상은 기존 강제 과열 보호
- 종료: 소스 EOF를 장치에 알린 뒤 PSRAM 큐를 실제로 비우고 STREAM_MODE를 종료

일반 YouTube/Instagram 페이지나 iframe의 영상 픽셀은 브라우저의 cross-origin 보안 정책 때문에 `/stream`이 직접 추출하지 않습니다. 그런 서비스의 페이지 URL이 아니라 브라우저가 직접 읽을 수 있는 미디어 소스가 필요합니다.

## 8. RGB 상태 LED

보드에 내장된 GPIO 21의 WS2812 RGB LED는 현재 상태를 계속 표시합니다. 정상 상태에서도 꺼지지 않으며, 설정 페이지에서 일반·야간 밝기와 사용 여부를 조절할 수 있습니다.

| 색상·동작 | 상태 |
|---|---|
| 민트색 점등 | Wi-Fi·인터넷·시간 동기화 정상 |
| 청록색 낮은 밝기 | 정상 Wi-Fi 절전 |
| 청록색 호흡 | 설정 AP 실행 중 |
| 파란색 호흡 | Wi-Fi 연결 중 |
| 보라색 호흡 | NTP 시간 동기화 중 |
| 빨간색 호흡 | Wi-Fi 연결 실패·오프라인 |
| 주황색 호흡 | Wi-Fi는 연결됐지만 NTP 실패 |
| 자홍색 빠른 호흡 | OLED 오류 |
| 주황색 빠른 호흡 | 칩 온도 경고 |
| 주황·빨강 계열의 더 빠른 호흡 | 온도 센서 연속 오류 |
| 빨간색 매우 빠른 호흡 | 과열 안전 모드 |
| 파란색 짧은 2회 점멸 | GitHub 업데이트 확인 중 |
| 흰색 짧은 2회 점멸 | 새 펌웨어 설치 선택 대기 |
| 보라색 빠른 호흡 | 펌웨어 다운로드·검증 중 |
| 빨간색 짧은 3회 점멸 | 업데이트 오류 |
| 흰색→청록색→빨간색 | BOOT 버튼 누름 단계 |
| 빨간색 빠른 호흡 | 공장 초기화 확인 |

## 9. 칩 온도 표시와 과열 보호

상단 상태 기호 왼쪽에 ESP32-S3 내부 온도를 `42C` 형식으로 표시합니다. 설정 포털의 **상단 상태 기호 옆에 ESP32 칩 온도 표시**를 끄면 평상시 온도만 숨겨지며, 과열 경고와 보호 기능은 계속 작동합니다. 온도 센서가 연속으로 실패하면 토글 상태와 관계없이 `!TC`가 표시됩니다. 이 값은 칩 내부 온도이므로 실내 온도계 값보다 높고 CPU·Wi-Fi 부하와 방열 상태의 영향을 받습니다.

| 내부 온도 | 동작 |
|---|---|
| 70°C 이상 | 온도 앞에 `!` 표시, RGB LED 주황색 경고 |
| 80°C 이상 | CPU를 80MHz로 낮춰 발열 완화 |
| 90°C 이상 10초 지속 | Wi-Fi와 설정 AP를 끄고 과열 안전 화면 유지 |
| 온도 센서 오류 3회 연속 | 오류 경고와 CPU 80MHz 감속 |
| 온도 센서 오류 약 60초 지속 | Wi-Fi와 설정 AP를 끄고 센서 안전 화면 유지 |
| 안전 모드에서 70°C 이하 60초 지속 | 정상 동작 및 Wi-Fi 자동 복귀 |
| 일반 감속 상태에서 75°C 이하 30초 지속 | 원래 CPU 주파수 복원 |

평상시에는 오차가 작은 20~100°C 측정 범위를 사용하고 고온 접근 시 50~125°C 범위로 자동 전환합니다. 이 임계값은 ESP32-S3 내부 센서를 이용한 MILESTONE의 보수적인 보호 정책이며, 주변 환경의 허용 온도와 같은 뜻이 아닙니다. 안전 모드는 자체 발열을 줄이고 자동 복구를 시도하지만 전원을 물리적으로 차단할 수는 없습니다. 온도가 내려가지 않거나 계속 상승하면 즉시 전원을 분리하고 전원 공급·배선·밀폐·통풍 상태를 점검하십시오.

## 10. 시리얼 로그 확인

Arduino IDE의 시리얼 모니터를 `115200 baud`로 열면 `[MILESTONE]`으로 시작하는 상태 로그를 볼 수 있습니다. Wi-Fi 비밀번호는 로그에 출력하지 않습니다.

Arch Linux 터미널에서 보려면 Arduino IDE의 시리얼 모니터를 먼저 닫은 뒤 다음을 사용할 수 있습니다.

```bash
picocom -b 115200 /dev/ttyACM0
```

종료는 `Ctrl+A`, 그다음 `Ctrl+X`입니다.

## 11. GitHub 인터넷 업데이트

업데이트 저장소는 다음 공개 저장소로 고정되어 있습니다.

```text
https://github.com/CXITRON/MILESTONE-Core
```

기기는 부팅 후 Wi-Fi 연결과 NTP 동기화를 먼저 완료해 시간과 디데이를 확정합니다. 최신 정식 Release 확인은 새 부팅에서 실제 NTP 응답을 받아 외부 연결을 확인한 뒤 시작하며, 확인 중에는 일반 화면을 유지하고 우상단에 `U`를 표시합니다. 계속 켜져 있는 경우 마지막 정상 확인 시각으로부터 7일 뒤 다시 확인합니다. DNS·TLS·일시적 HTTP 오류는 짧은 백오프로 최대 3회 확인한 뒤 10분 후 자동 재시도하며, 명백한 비정상 Release 응답이나 실제 OTA 설치 실패는 6시간 뒤 재시도합니다. 자동 확인은 설치까지 자동으로 실행하지 않습니다.

부팅 로고는 네트워크 응답을 기다리지 않고 약 3초 뒤 종료됩니다. NTP 서버 응답이 느리면 일반 화면의 우상단에 `T`를 표시한 채 백그라운드에서 최대 60초 동안 대체 서버까지 시도합니다. 저해상도 OLED에서 시간 동기화와 업데이트 확인을 혼동하지 않도록 두 상태를 각각 `T`와 `U` 문자로 구분합니다. NTP 성공 전에는 GitHub 업데이트 확인을 시작하지 않습니다.

업데이트 확인이 정상적으로 끝나 현재 버전이 최신이면 별도 전체 화면을 표시하지 않습니다. 확인 실패 또는 새 버전 발견 때만 OLED 전체 화면에 안내가 나타나며, 새 버전 안내는 15초 동안 유지됩니다.

- BOOT 버튼을 1초 미만으로 짧게 눌렀다 놓기: 설치
- 아무 조작 없이 15초 경과: 이번에는 보류
- 설정 포털: `지금 업데이트 확인` 및 `업데이트 설치` 사용 가능

업데이트 파일은 HTTPS 인증서로 서버를 검증하고, manifest에 기록된 크기와 SHA-256이 모두 일치해야만 다음 부팅 펌웨어로 확정됩니다. HTTP로 낮아지는 주소, 비정상 manifest, 용량 부족, RAM 부족, 연결 중단, 고온·온도 센서 오류 상태에서는 설치하지 않습니다.

GitHub와 Release 자산 전달 호스트의 인증서 체인을 검증할 수 있도록 DigiCert Global Root G2, ISRG Root X1, USERTrust RSA/ECC 루트를 포함합니다. 별도의 `hostByName()` 사전 조회는 하지 않습니다. HTTPS 연결이 자체적으로 DNS 조회를 수행하므로 중복 조회를 없애 블로킹과 추가 실패 지점을 줄였습니다. 실제 HTTPS 요청은 리디렉션을 제한된 횟수만 따라가며, 요청 당시의 DNS 서버·RSSI·여유 RAM과 HTTP/TLS 오류를 시리얼 로그에 기록합니다. Wi-Fi 연결 해제 reason code와 DHCP 완료 후 IP·게이트웨이·DNS도 기록해 공유기 문제와 GitHub 요청 문제를 분리할 수 있습니다.

`동기화 뒤 Wi-Fi 끄기`를 사용하면 업데이트 확인 시에는 저장된 Wi-Fi에 잠시 연결하고, 작업이 끝난 뒤 Wi-Fi 자체를 다시 끕니다. 이 옵션을 사용하지 않는 항상 연결 모드에서는 ESP 모뎀 절전을 별도로 켜지 않고 무선부를 깨어 있게 유지해 설정 모드와 콜드 부팅 STA 모드의 통신 조건을 최대한 같게 맞춥니다. HTTPS 인증서의 유효기간을 확인하려면 정확한 시간이 필요하므로, 부팅 시간 동기화를 꺼 두었더라도 전원이 완전히 끊겨 시각이 유효하지 않으면 업데이트 확인을 위해 NTP를 한 번 수행합니다.

### Release 자동화 (권장)

반복되는 Taildrop 수신, 검증, 테스트, 빌드, Git push/tag, GitHub Release 게시 절차는 `milestone-release`로 통합합니다. 프로젝트에서 한 번 설치하면 됩니다.

```bash
./tools/install-milestone-release.sh
```

PC의 현재 프로젝트를 직접 수정한 경우에는 **MILESTONE_Core 프로젝트 루트에서** 실행합니다.

```bash
cd /run/media/citron/T7/Documents/Dev/MILESTONE_Core
milestone-release local 1.10.6 "Fix raw stream request metadata"
```

Tailscale Taildrop으로 `MILESTONE_Core_1.10.6.zip`을 받은 경우에는 교체 대상 프로젝트 디렉터리 밖의 정상적으로 존재하는 디렉터리에서 실행합니다. 기존 프로젝트가 교체될 때 현재 셸 경로가 사라지는 문제를 방지하기 위해 `/tmp`로 이동하는 방식을 권장합니다.

```bash
cd /tmp && milestone-release taildrop 1.10.6 "Fix raw stream request metadata"
```

Taildrop 모드는 ZIP을 라이브 프로젝트에 바로 덮어쓰지 않습니다. 별도 staging 디렉터리에서 구조·버전·Git 상태를 검사하고 테스트와 ESP32 릴리즈 빌드까지 성공한 뒤에만 프로젝트를 교체합니다. GitHub 게시 전 실패하면 기존 프로젝트를 자동 복구합니다. `--dry-run`은 테스트/빌드까지만 수행하고 로컬 프로젝트·Git·GitHub를 변경하지 않습니다. `--yes`를 사용하지 않는 기본 동작은 최종 게시 직전에 한 번 확인합니다.

### Taildrop 원격 이력 조정

`milestone-release taildrop`은 ZIP의 HEAD가 최신 `origin/main`을 포함하지 않으면 공통 릴리즈 태그를 확인한 뒤, 원격의 실제 펌웨어 버전과 일치하는 Taildrop 쪽 최신 커밋을 기준점으로 삼아 그 이후 변경만 최신 `origin/main` 위에 재적용합니다. 이미 원격에 반영된 같은 버전의 준비 커밋을 다시 재생하지 않습니다. 충돌하거나 안전한 버전 기준점을 찾지 못하면 기존 프로젝트를 교체하거나 원격에 push하지 않고 중단합니다. 강제 push는 사용하지 않습니다. 릴리즈 성공 후 설치된 `milestone-release` 명령도 새 프로젝트의 도구로 갱신합니다.

코드 에이전트는 `AGENTS.md`의 릴리즈 규칙을 우선 따라야 하며, 새 버전을 준비한 뒤 사용자에게 위 `local` 또는 `taildrop` 명령 중 상황에 맞는 정확한 한 줄을 안내해야 합니다.

### Release 파일 만들기 (저수준 명령)

`tools/make-release.sh`는 `milestone-release` 내부에서도 사용하는 정식 저수준 빌드 스크립트입니다. 설치된 `arduino-cli` 또는 Arduino IDE 내장 CLI를 사용해 권장 보드, OTA 파티션, 공식 온보드 PSRAM 활성화 설정으로 직접 컴파일합니다. IDE에서 BIN을 따로 내보낼 필요가 없습니다.

```bash
chmod +x tools/make-release.sh
./tools/make-release.sh 1.10.6 "Fix raw stream request metadata"
```

기본 설명을 사용하려면 `./tools/make-release.sh 1.10.6`만 실행할 수 있습니다. 다른 CLI를 사용해야 할 때는 `ARDUINO_CLI=/경로/arduino-cli`로 지정합니다. 임의로 내보낸 BIN을 받지 않으므로 잘못된 PSRAM·파티션 설정이 릴리스에 섞이지 않습니다.

다음 두 파일이 `release/`에 생성됩니다.

```text
release/MILESTONE_Core.bin
release/MILESTONE_Core.json
```

스크립트가 BIN의 실제 바이트 수와 SHA-256을 계산하므로 JSON을 손으로 수정하지 않는 것이 안전합니다. 릴리스 빌드 전에 `tools/test-core.sh`가 자동 실행되어 하드웨어 독립 핵심 로직과 포털/API 계약, **문서 기준 버전·README 버전 이력 순서 동기화**까지 모두 통과해야 다음 단계로 진행됩니다. 테스트만 따로 실행하려면 다음 명령을 사용합니다.

```bash
./tools/test-core.sh
```

### GitHub Release 게시

일반적인 게시에는 위의 `milestone-release`를 사용합니다. 아래 수동 절차는 자동화 도구를 복구하거나 디버깅해야 할 때만 참고합니다.

1. 저장소의 `Releases`에서 `Draft a new release`를 선택합니다.
2. 버전과 동일한 태그를 만듭니다. 예: `v1.10.6`
3. Release 제목을 `MILESTONE Core v1.10.6`로 지정합니다.
4. `release/MILESTONE_Core.bin`과 `release/MILESTONE_Core.json`을 첨부합니다.
5. Pre-release가 아닌 최신 정식 Release로 게시합니다.
6. 1.9.9 이상이 설치된 기기에서 1.10.6 OTA 업데이트와 기존 설정·Wi-Fi·롤백 보호·진단 이력·커스텀 미디어 및 전용 스트리밍 모드 동작을 검증합니다.

두 파일의 이름은 모든 Release에서 정확히 같아야 합니다. 초안이나 Pre-release는 `latest` 업데이트 대상으로 사용하지 않습니다.

최초 1.5.0 배포에서는 먼저 1.5.0 BIN과 manifest를 정식 Release로 게시한 뒤 Arduino IDE 일반 업로드를 실행하는 순서를 권장합니다. USB 업로드 직후 자동 재부팅될 때 이미 1.5.0 manifest가 존재하므로 정상적으로 `최신 버전과 동일` 판정을 확인할 수 있습니다. Release를 나중에 게시해도 기기가 손상되지는 않지만, 첫 부팅에서는 manifest HTTP 404 오류가 표시됩니다.

### 1.5.1 설치 및 후속 OTA 테스트

초기 1.5.0 빌드의 OTA 다운로드 함수에는 4KB 지역 버퍼와 TLS·HTTP 객체가 같은 loopTask 스택에 놓여 설치 시작 직후 스택 부족으로 재부팅될 수 있는 문제가 있습니다. 이미 설치된 1.5.0의 다운로드 코드는 원격 Release 파일만 바꿔서는 수정할 수 없으므로, 수정된 1.5.1은 한 번 Arduino IDE 일반 업로드로 설치해야 합니다. 이후 1.5.2 이상 Release부터 실제 인터넷 업데이트를 검증합니다. 1.5.1 부팅에서는 로고가 약 3초 유지되고, GitHub의 최신 버전과 일치하면 `UP TO DATE` 화면이 약 1초 표시됩니다.

업데이트가 다운로드·검증 단계에서 실패하면 현재 실행 중인 펌웨어는 그대로 유지됩니다. 1.8.0부터는 OTA 직전 현재 앱 슬롯을 롤백 대상으로 기록하고, 새 이미지가 부팅된 뒤 약 10초 동안 정상 루프가 유지되기 전에 다시 부팅되면 이전 OTA 슬롯을 자동으로 다시 선택합니다. 부트로더 롤백 기능이 활성화된 환경에서는 앱 유효 표시도 함께 수행합니다. 다만 새 이미지가 애플리케이션 롤백 가드까지 도달하기 전에 완전히 멈추는 경우까지 자동 복구하려면 롤백 기능이 활성화된 부트로더가 필요합니다.

## 구현 범위

- 기존 7개 화면, 커스텀 미디어 슬롯, 선택 순환 모드
- 문구+시간 화면 및 디데이·시간·날짜·문구 종합 화면
- 문구+시간 화면 상단에 오늘 날짜 표시
- 디데이(시간) 화면에서 날짜와 시각을 고정된 두 줄로 표시
- 한국어 U8g2 글꼴, 긴 문구 스크롤
- Asia/Seoul NTP 및 마지막 디데이 보존
- AP+STA Wi-Fi 시험 후 자격 증명 확정
- 반응형 로컬 설정 포털과 captive portal
- 슬라이더와 숫자 입력칸이 연동되는 OLED·LED 밝기 조절
- 최근 성공 순 최대 8개 Wi-Fi 저장, 자동 대체 연결, 개별 삭제
- WPA2-Enterprise AP 감지 및 사용자명·비밀번호 기반 PEAP 연결, WPA3-Enterprise CA 필요 AP 별도 표시
- 기존 Personal/Enterprise Wi-Fi와 화면 순서를 보존하는 스키마 9 및 A/B Wi-Fi 저장 Bank
- Wi-Fi를 보존하는 표시 설정 기본값 복원
- 선택 가능한 ESP32-S3 내부 온도 표시와 상시 과열 보호
- 밝기·야간 밝기·번인 이동·자동 화면 끄기
- Wi-Fi 절전 및 주기적 재연결
- GPIO 21 WS2812 상시 상태 LED와 야간 밝기
- 비동기 Wi-Fi 검색으로 검색 중 화면·버튼 처리 유지
- 자동 순환과 화면 자동 꺼짐의 독립 동작
- 긴 문구의 두 줄 표시·정렬·초과 스크롤 수정
- NVS 설정 보존과 2단계 공장 초기화
- 부팅 시 및 주 1회 GitHub Release 업데이트 확인
- HTTPS 인증서·크기·SHA-256 검증 후 OTA 설치
- OLED·RGB LED·설정 포털의 업데이트 상태와 진행률 표시
- 일시적 업데이트 확인 실패 10분·설치/구조적 실패 6시간 재시도와 Wi-Fi 절전 자동 복귀
- 5페이지 기기 세부정보 화면과 설정 포털의 실시간 시스템 상태
- 별도 NVS namespace에 최근 16개 핵심 부팅·네트워크·NTP·OTA·롤백·온도 이벤트를 보존하는 Diagnostics & Health 이력
- 설정 포털의 진단 상태·최근 오류·최고 온도 표시, 진단 텍스트 복사와 이력 전용 초기화
- 브라우저에서 사진·GIF·로컬 영상을 128×128 1비트 MSM1로 변환하는 OLED 미리보기
- LittleFS 160KiB 한도, 최대 64개, CRC 검증, 임시 업로드와 A/B 인덱스를 사용하는 커스텀 미디어 저장·재생
- 저장형 미디어와 분리된 `/stream` 전용 페이지, 브라우저 전체 선변환, raw binary 전송을 사용하는 실시간 스트리밍
- STREAM_MODE에서 일반 백그라운드 기능을 격리하고 PSRAM 32프레임 링버퍼·부분 OLED 갱신·프레임 드롭·80°C 스트림 상한으로 영상 수신/표시에 집중
- 라이브 송신은 20프레임 초기 충전 후 큐 12프레임 이하에서 최대 8프레임씩 22~24프레임까지 빠르게 보충하며 ESP32만 재생 클록을 담당
- 비차단 3초 부팅 로고와 우상단 NTP `T`·업데이트 `U` 상태 아이콘

## v1.10.7 업데이트 안내

v1.10.6 실기기에서 20fps와 24fps 모두 심한 끊김이 관찰되어, 송신 공급 부족 가능성을 우선 제거하기 위해 브라우저 송신 스케줄러를 다시 설계했습니다. 기존 방식은 32프레임 PSRAM 큐가 있어도 초기 8프레임만 채우고, 정상 구간에서 2프레임 POST마다 ACK를 기다리며 브라우저 시간축에 맞춰 조금씩 보내는 구조여서 HTTP 왕복 지연이 그대로 버퍼 언더런으로 이어질 수 있었습니다.

v1.10.7에서는 브라우저가 재생 타이밍을 직접 pacing하지 않고 **PSRAM 큐의 수위만 관리**합니다. 시작 전에 20프레임을 선충전하고, 재생 중 큐가 12프레임 이하로 내려가면 최대 8프레임 배치로 연속 전송해 22~24프레임 수준까지 회복합니다. ACK 사이에는 마지막 큐 수위와 목표 FPS를 이용해 예상 잔여 프레임을 계산하므로 별도 상태 polling을 추가하지 않습니다. 스트림 상태에는 HTTP RTT도 함께 표시해 공급 지연과 OLED 출력 병목을 실기기에서 구분할 수 있게 했습니다. ESP32는 기존과 동일하게 유일한 재생 클록을 유지하고, 언더런 후에는 12프레임을 다시 확보한 뒤 재생을 재개합니다. STREAM_MODE, raw header metadata, PSRAM 32프레임 큐, OLED 출력, 열·자원 보호는 그대로 유지합니다.

정식 버전: `1.10.7`

## v1.10.6 업데이트 안내

v1.10.5 실기기 검증에서 스트리밍 시작 직후 `초기 프레임 선버퍼링 중…` 다음 `invalid-request`로 중단되는 문제를 수정했습니다. v1.10.5의 raw `application/octet-stream` push는 session·sequence·frame-count를 URL query로 전달했지만, Arduino-ESP32 3.3.11 `WebServer`의 raw-body callback 경로에서는 해당 query가 `server.arg()`에 파싱되지 않아 첫 prebuffer push가 거절될 수 있었습니다.

v1.10.6에서는 raw 프레임 body는 그대로 유지하면서 메타데이터를 명시적으로 수집하는 `X-MILESTONE-Session`, `X-MILESTONE-Seq`, `X-MILESTONE-Count` 요청 헤더로 이동했습니다. 브라우저의 transport-loss 재시도는 동일 sequence·header·body를 재사용하고 ESP32의 기존 idempotent duplicate 처리도 유지합니다. STREAM_MODE, PSRAM 32프레임 링버퍼, 선변환, OLED 출력 및 열·자원 보호 구조는 변경하지 않았습니다.

정식 버전: `1.10.6`

## v1.10.5 업데이트 안내

실시간 스트리밍을 기존 설정 포털의 부가 기능에서 **독립 STREAM_MODE**로 재설계합니다. 메인 설정 포털은 `/stream` 전용 페이지로 연결만 하며, 브라우저가 영상을 먼저 128×128 1비트 프레임으로 전체 선변환한 뒤 정속으로 전송합니다. 스트리밍 중에는 ESP32가 일반 NTP/OTA/진단/화면 순환/저장형 미디어/일반 LED 처리 경로를 실행하지 않고, 설정 AP·raw binary 수신·PSRAM 링버퍼·OLED 출력·BOOT 중지·저주기 온도 보호에 집중합니다.

- 전용 `/stream` 페이지와 전체 선변환 후 재생
- 5/10/15/18/20fps, 24fps Experimental
- `multipart/FormData` 스트리밍 제거, `application/octet-stream` raw body 사용
- 최대 8프레임 raw push, 32프레임 PSRAM 링버퍼, 초기 8프레임 선버퍼 및 4프레임 재버퍼
- 정상 재생 구간은 2프레임 단위로 pacing해 HTTP 요청 수와 burst를 줄이고, ACK 유실 시 같은 sequence/body를 안전하게 재전송
- 마지막 프레임 전송 후 `/api/stream/finish`로 EOF를 선언하고 ESP32가 실제 큐를 끝까지 비운 뒤 STREAM_MODE 종료
- hot path JSON 대신 28바이트 binary ACK
- 동일 sequence 재전송을 중복 프레임 없이 처리
- OLED 프레임 diff에 따라 U8g2 tile 부분 갱신 또는 전체 flush 선택
- 스트리밍 중 일반 펌웨어 background subsystem 조기 차단 및 종료 후 복원
- CPU 240MHz 성능 모드, 75°C 경고 / 80°C 스트림 중단 / 85°C 기존 강제 보호
- OTA의 2KiB 전송 버퍼는 상시 내부 SRAM 점유를 없애고 실제 OTA 다운로드 동안에만 동적 할당
- internal heap/largest block/loopTask stack 감시 및 자원 부족 시 재부팅 대신 스트림 종료
- 비정상 재부팅 시 이전 live stream 중단 마커와 reset reason 진단 유지

저장형 사진/GIF/영상 MSM1 기능과 1024프레임·160KiB 제한은 그대로 유지되며, 이 제한은 스트리밍 모드에는 적용되지 않습니다. 스트리밍 영상 데이터는 LittleFS에 저장하지 않습니다.

정식 버전: `1.10.5`

## v1.10.3 업데이트 안내

실기기에서 스트리밍 재생 진입 후 심한 끊김과 재부팅이 관찰된 경로를 안정화합니다. 10fps 이상에서는 4프레임 단위 전송을 사용해 HTTP multipart 요청 횟수를 줄이고, 스트리밍 hot path의 JSON 응답 버퍼를 재사용하며, heap/연속 heap/loopTask stack 여유가 위험 수준으로 내려가면 기기 전체가 재부팅되기 전에 스트림만 중단합니다.

## v1.10.2 업데이트 안내

- 실시간 스트리밍 시작 시 설정 AP를 명시적으로 유지하도록 수정
- Wi-Fi 시험 성공 후 예약된 포털 종료가 스트리밍 세션을 끊지 않도록 수정
- 스트리밍 중 일반 설정 AP 타임아웃 억제
- 첫 스트림 프레임이 실제 수신되기 전까지 OLED의 MILESTONE SETUP 화면 유지

정식 버전: `1.10.2`

## v1.10.1 업데이트 안내

- 1.10.0 스트리밍 시작 시 설정 AP 응답성 저하 보완
- 기본 10fps, OLED 실제 표시 최대 15fps, 입력 소스 최대 24fps
- 스트리밍 중 포털 타임아웃 연장 및 stale 판정 5초

정식 버전: `1.10.1`

## v1.10.0 업데이트 안내

- 설정 포털에 **실시간 스트리밍** 영역 추가
- 휴대폰/PC의 로컬 동영상 또는 브라우저가 직접 읽을 수 있는 영상 URL을 128×128 1비트 프레임으로 실시간 변환
- 스트림 프레임은 LittleFS에 저장하지 않고 PSRAM 기반 12프레임 링버퍼로만 처리해 1024프레임·160 KiB 저장형 미디어 제한 제거
- 프레임 전송은 최대 4장을 한 HTTP multipart 배치로 묶어 요청 횟수를 줄이고 5/10/15/20fps 및 24fps 실험 모드 제공
- 브라우저 송신 FPS·건너뛴 프레임과 기기 수신·표시·드롭·언더런·버퍼 상태를 포털에 실시간 표시
- 기기가 일정 이상 뒤처지면 오래된 큐 프레임을 버려 누적 지연 대신 현재 영상 시간에 가깝게 유지
- 포털 종료, 3초 이상 프레임 미수신, 온도 보호, 펌웨어 작업 진입 시 스트림 자동 종료
- 저장형 미디어 기능과 MSM1 포맷은 그대로 유지하고 스트리밍 중에는 OTA/미디어 저장 변경과 상호 배제
- YouTube 일반 페이지/iframe URL은 브라우저가 cross-origin 플레이어 픽셀을 직접 읽을 수 없어 직접 입력 대상으로 지원하지 않으며, 로컬 파일 또는 CORS 허용 직접 영상 URL을 사용

## v1.9.9 업데이트 안내

- 브라우저 변환기의 300프레임 고정 제한을 제거하고 MSM1 포맷의 실제 상한인 1024프레임까지 허용
- 영상/GIF 변환의 40초 고정 시간 제한 제거
- 요청 길이는 원본 남은 길이, `1024 / FPS`, 160 KiB 변환 결과 한도에 의해 자동 제한
- 이론상 최대 길이는 5fps 204.8초, 8fps 128초, 10fps 102.4초이며 실제 영상은 압축 결과에 따라 160 KiB 한도에 먼저 도달할 수 있음
- 포털에 프레임/시간/용량 제한 관계를 명시하고 레거시 300프레임·40초 제한이 다시 들어오지 않도록 계약 테스트 추가

## v1.9.8 업데이트 안내

- 개별 미디어 삭제에서 `confirm()` 팝업 의존성을 제거하고 미디어 목록 컨테이너의 단일 `click` 이벤트 위임으로 처리
- 첫 탭에서 목록 바로 위 상태를 `삭제 확인`으로 즉시 변경하고 버튼 문구를 `다시 눌러 삭제`로 변경
- 8초 안에 같은 삭제 버튼을 다시 누른 경우에만 실제 `/api/media/delete` POST 요청 전송
- 동적으로 생성되는 미디어 버튼에 `type="button"`을 명시하고 `addEventListener()`로 이벤트 연결
- 전체 미디어 삭제도 동일한 2단계 탭 확인 방식으로 통일해 `prompt()` 의존성 제거
- 삭제 요청 뒤 HTTP 응답과 목록 재조회로 실제 ID 소멸까지 검증하는 v1.9.7 서버 측 안전장치는 유지
- 테스트가 `deleteMedia()` 함수를 직접 호출하던 허점을 제거하고, 미디어 목록의 delegated `click` 이벤트를 두 번 발생시켜 실제 UI 경로를 검증

## v1.9.7 업데이트 안내

- 삭제 API 응답에 내부 처리 stage를 포함하고, 성공 응답 뒤 목록을 다시 받아 실제 ID 소멸을 검증
- 카탈로그 기록이 공간 부족으로 먼저 실패하면 대상 파일을 제거한 뒤 카탈로그 기록을 재시도
- 삭제 결과 전용 상태 영역을 미디어 목록 위에 추가
- 다만 당시 회귀 테스트가 실제 삭제 버튼 클릭이 아니라 삭제 함수를 직접 호출해, 모바일 브라우저의 버튼 활성화 경로 문제를 놓침

## v1.9.6 업데이트 안내

- 개별 미디어 삭제 전에 재생 파일 핸들을 닫아 LittleFS 삭제와의 충돌 가능성을 제거
- 카탈로그 갱신 후 실제 파일 삭제가 실패하면 이전 카탈로그를 복구하도록 시도
- 삭제 중·성공·실패 메시지를 기존 미디어 정보 영역에 출력하도록 추가

## v1.9.5 업데이트 안내

- iPhone/iPad에서 iCloud 원본을 가져오지 못한 영상이 사진 선택 화면에서 재생 실패·`!` 표시와 함께 `cancel · files=0`으로 끝나는 실제 원인을 UX에 반영
- `cancel · files=0` 발생 시 더 이상 180초를 기다리지 않고 즉시 **파일 전달 실패**로 판정해 사용자가 대기 중인지 실패한 것인지 명확히 구분
- 직접 취소한 경우가 아니라면 모바일 데이터/인터넷을 켜거나 영상을 먼저 재생·다운로드한 뒤 다시 선택하라는 안내 추가
- 정상적인 무이벤트 지연에 대해서만 기존 180초 watchdog을 유지
- 선택 → 파일 전달 → 파일 읽기 확인 → 변환 → 기기 업로드의 현재 단계를 진단 영역에 계속 표시
- 핸드폰에서 사진 앱 동영상을 선택하기 전에 iCloud 원본과 데이터 연결 조건을 확인할 수 있는 안내 문구 추가

## v1.9.4 업데이트 안내

- 영상 선택 뒤 발생한 WebKit `cancel · files=0`을 사용자 취소로 즉시 확정하지 않고 180초 후속 전달 감시 유지
- `cancel` 이후 늦게 `File`이 생성되는 상태 전이를 회귀 테스트에 추가
- 네이티브 선택기를 여는 첫 클릭에서 빈 파일 입력값을 재설정하지 않아 WebKit 활성화 중 상태 변경 방지
- 화면에 `files=0 · cancel` 응답과 후속 감시 경과시간을 별도로 표시

## v1.9.3 업데이트 안내

- 사진 앱용 동영상 선택과 형식 제한이 없는 파일 앱 선택을 별도 경로로 제공
- 파일 입력의 `input`·`change`·`cancel` 이벤트를 모두 기록하고, 이벤트가 누락돼도 `files` 폴링으로 전달된 파일 감지
- 선택 시작부터 브라우저 `File` 전달까지 경과 시간을 표시하고 180초 watchdog으로 파일 제공자 지연과 실제 실패 구분
- 전달된 파일의 첫 데이터를 직접 읽어 0바이트·원본 미다운로드·파일 접근 실패를 변환 전에 분리 진단
- 영상 메타데이터 분석 시간, 현재 변환 프레임, 전체 프레임, 변환 경과시간을 계속 표시
- 사용자 에이전트와 선택 이벤트 이력을 담은 복사 가능한 파일 선택 진단 추가
- `input` 단독·`change` 중복·이벤트 누락·전달 timeout 상태를 실행하는 파일 선택 상태 머신 테스트 추가

## v1.9.2 업데이트 안내

- Safari가 선택한 동영상을 입력에서 폐기하지 않도록 네이티브 파일 형식 필터 제거
- 단일 파일 선택 후 JavaScript에서 사진·GIF·동영상을 판정하도록 변경
- iPad의 사진·파일 앱에서 설정 페이지로 파일을 끌어놓는 우회 입력 경로 추가
- Safari가 파일을 전달하지 않은 경우 변환 오류와 구분되는 메시지 표시
- `CoreMedia.h/.cpp` 호스트 테스트와 포털/API/마이그레이션 계약 테스트 추가

## v1.9.1 업데이트 안내

- 사진/GIF와 동영상 파일 선택기를 분리해 iOS·모바일 파일 선택 호환성 개선
- MIME 정보가 없거나 불완전한 MP4·M4V·MOV 파일을 확장자로 안전하게 판정
- 동영상 메타데이터·프레임 로딩에 시간 제한과 단계별 상태 표시를 추가해 무한 대기 방지
- 변환 실패 시 선택 파일과 오류 원인을 유지해 재시도 가능

## v1.9.0 업데이트 안내

- 설정 포털에서 JPEG·PNG·WebP 등 브라우저 지원 사진, GIF, 로컬 영상 파일을 외부 서버 없이 128×128 흑백으로 변환하고 확대 미리보기 후 업로드
- 화면 채우기/전체 보기, 밝기·대비·반전, 투명 배경, 임계값·Bayer·Floyd–Steinberg 디더링과 GIF/영상 5·8·10fps 및 영상 시작 지점 지원
- 첫 프레임 raw와 후속 XOR-RLE delta, CRC32, 엄격한 길이·프레임·duration 검증을 갖춘 `MSM1` 전용 형식 추가
- 실제 LittleFS 크기 기반 최대 160KiB·64개 제한, 임시 파일 검증 후 rename, A/B 세대 인덱스, 명시적 저장소 복구 제공
- View 7과 TopMode 8을 추가하되 기존 ID를 유지하고, 스키마 8→9 마이그레이션에서 기존 순환 순서·mask·Wi-Fi·일반 설정 보존
- 미디어 업로드와 OTA를 상호 배제하고 손상·마운트·PSRAM 실패가 핵심 부팅·시간·네트워크·업데이트 기능을 막지 않도록 격리

## v1.8.3 업데이트 안내

- 마지막 이벤트 하나가 아니라 최근 event/detail 조합별로 60초 중복 억제 상태를 추적해 `A → B → A` 형태의 교차 반복도 불필요하게 Flash에 기록하지 않도록 수정
- 진단 이력을 임시 복사본에 먼저 추가하고 NVS 저장 성공 후에만 RAM 이력을 교체하는 transaction 방식으로 변경해 저장 실패 시 RAM/NVS 이력 불일치 방지
- 부팅 후 단순 10초 경과가 아니라 OLED·NVS가 준비되고 설정 AP·온라인·오프라인·Wi-Fi 절전 등 안정된 런타임 상태에 도달한 뒤에만 `BOOT_VALIDATED` 기록
- `millis()` overflow, 교차 이벤트 중복 억제, 고정 크기 suppression table 교체 동작을 호스트 회귀 테스트에 추가
- 설정 스키마 8과 진단 이력 저장 형식 1을 그대로 유지하고 Wi-Fi/NTP/OTA/rollback/온도 보호 상태 전이 순서는 변경하지 않음

## v1.8.2 업데이트 안내

- 최근 16개의 중요 부팅·Wi-Fi·NTP·OTA·롤백·온도 이벤트를 고정 크기 ring buffer로 보존하는 Diagnostics & Health 계층 추가
- 진단 이력을 일반 설정과 분리된 `milestone_diag` NVS namespace에 저장하고 checksum·버전·크기 검증으로 손상된 이력은 안전하게 초기화
- 동일 이벤트/detail의 짧은 반복은 60초 동안 억제하고 매 loop·RSSI 변화·화면 전환은 기록하지 않아 Flash write 횟수 제한
- Wi-Fi 연결 성공 시 연결 소요시간·RSSI를 저장하고, 연결 실패/해제 시 가능한 경우 ESP32 드라이버 disconnect reason code를 함께 기록
- NTP 성공 시 동기화 소요시간을 기록하고, NTP 전 부팅 이벤트는 epoch 0 + uptime으로 안전하게 표현
- OTA 시작·실패·재부팅 준비와 rollback arm·candidate boot·rollback 실행·확정을 기존 상태 전이 결과 지점에서 기록
- 현재 부팅 세션의 최고 칩 온도와 온도 경고·CPU 감속·안전모드·센서 오류·복구 이벤트 기록
- 설정 포털에 `/api/diagnostics`와 별도 Diagnostics 영역을 추가하고 최근 오류/이력 표시, **진단 정보 복사**, **진단 이력 지우기** 제공
- 공장 초기화는 별도 진단 namespace도 삭제하지만 **표시 설정 기본값 복원**은 진단 이력을 유지
- `CoreDiagnostics.h/.cpp` 호스트 테스트와 포털/API 계약 테스트를 `tools/test-core.sh`에 추가
- 설정 스키마는 8 그대로 유지하고 Wi-Fi/NTP 판단 순서, OTA 다운로드 transaction, rollback 알고리즘, `loadConfig()` migration 구조는 변경하지 않음

## v1.8.1 업데이트 안내

- Arduino 프레임워크에 의존하지 않는 `CoreLogic.h`/`CoreLogic.cpp`를 추가하고 semantic version, ISO 날짜, D-day 날짜 차이, 화면 순환 순서, SHA-256 형식 검증을 실제 펌웨어와 호스트 테스트가 공동 사용
- `tools/test-core.sh`와 `tests/test_core_logic.cpp`를 추가해 Arch Linux의 일반 `g++` 환경에서 핵심 순수 로직을 빠르게 회귀 검사
- D-day 날짜 차이 계산을 `mktime()`/`difftime()` 기반 시간 계산에서 순수 civil-date 일수 계산으로 분리해 날짜 경계 로직을 직접 테스트 가능하게 변경
- Release manifest 생성기가 출력할 수 있는 표준 JSON escape `\b`, `\f`를 펌웨어 parser도 정상 해석하도록 producer/consumer 계약 불일치 수정
- `tools/make-release.sh`가 정식 Arduino 빌드 전에 호스트 핵심 로직 테스트를 실행하도록 강화
- 설정 스키마 8, NVS key, Wi-Fi/NTP 상태기계, OTA 다운로드 순서, 앱 레벨 롤백 상태기계, OLED/UI 동작은 변경하지 않음
- 대규모 전역 상태 구조체화와 긴 상태기계 함수 분해는 회귀 위험 대비 실익이 부족해 이번 버전에서 의도적으로 제외

## v1.8.0 업데이트 안내

- OTA 설치 전에 현재 실행 중인 앱 슬롯과 버전을 롤백 원본으로 기록
- 새 펌웨어 첫 부팅을 `validating` 상태로 두고 약 10초 동안 정상 루프 실행을 확인
- 검증 완료 전에 새 펌웨어가 다시 부팅되면 이전 OTA 슬롯을 부팅 대상으로 복원하고 자동 재부팅
- 1.7.0에서 1.8.0으로 처음 올라오는 경우에도 반대편 OTA 슬롯을 자동 탐색해 이전 펌웨어를 롤백 대상으로 채택
- 설정 포털의 업데이트 상태에 롤백 보호 여부, 이전 버전, 부팅 시도 및 최근 롤백 결과를 표시
- 부트로더 롤백 기능이 활성화된 환경에서는 기존 `esp_ota_mark_app_valid_cancel_rollback()` 확인 절차도 함께 사용
- Wi-Fi 연결, DHCP 안정화, NTP, GitHub manifest 확인 및 OTA 다운로드/검증 상태기계는 기존 동작 유지

앱 레벨 롤백은 새 펌웨어가 `Preferences` 초기화와 롤백 가드까지 실행된 이후 발생한 재부팅을 보호합니다. 새 이미지가 그보다 앞에서 완전히 부팅 불능 상태가 되는 경우까지 자동 복구하려면 `CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE`이 활성화된 부트로더가 필요합니다.

## v1.7.0 업데이트 안내

- 기능 변경 없이 1.6.1 구현부를 역할별 `*.inc` 파일로 기계적으로 분리
- 모든 기존 함수 본문을 1.6.1과 바이트 단위로 동일하게 유지
- 함수 정의 순서와 단일 translation unit 구조를 그대로 유지해 전역 초기화·링커·호출 순서 변화 방지
- 네트워크·NTP·GitHub 업데이트 확인·OTA 로직을 수정하지 않음
- 설정 스키마 8 및 기존 저장 Wi-Fi/NVS 구조를 그대로 유지
- 실제 펌웨어 버전 문자열만 1.7.0으로 변경

## v1.6.1 업데이트 안내

- 펌웨어 버전을 1.6.1로 변경하고 설정 스키마를 8로 확장
- 기존 `wifi_ssidN`/`wifi_passN` 저장 영역을 Bank A로 그대로 인정해 1.6.0 이하의 저장 Wi-Fi를 재입력 없이 보존
- Wi-Fi 목록 변경 시 비활성 Bank에 전체 목록을 기록·검증한 뒤 마지막에 활성 Bank 한 바이트만 전환하는 A/B 저장 방식 적용
- 화면·시간·LED 일반 설정 저장에서 Wi-Fi 자격 증명 재기록을 제거해 불필요한 NVS 쓰기 감소
- `Preferences.begin()` 실패를 감지하고 설정 포털 상태에 NVS 준비 여부와 여유 entry 수를 표시
- WPA2-Enterprise PEAP를 정식 지원 범위로 명확히 하고 WPA3-Enterprise/CA 필수 AP는 스캔에서 지원 불가로 안내
- 기존 Wi-Fi 연결·DHCP·NTP·GitHub 업데이트 확인·OTA 상태기계는 변경하지 않음

## v1.6.0 업데이트 안내

- 펌웨어 버전을 1.6.0으로 변경하고 설정 스키마를 7로 확장
- Enterprise 네트워크 검색 표시 및 PEAP 사용자명·비밀번호 인증 추가 (WPA3-Enterprise 정식 지원 범위는 1.6.1에서 명확히 분리)
- Enterprise identity 선택 입력 지원, 미입력 시 사용자명을 outer identity로 사용
- 저장 네트워크마다 Personal/Enterprise 보안 방식과 Enterprise 사용자명·identity를 함께 기억
- 1.5.11 이하의 기존 `wifi_ssidN`·`wifi_passN` 키를 그대로 유지하고 Personal 항목에는 새 Enterprise 메타데이터 키를 만들지 않아 저장된 Wi-Fi와 비밀번호를 자동 보존
- 표시 설정 기본값 복원 시 Enterprise 자격 증명을 포함한 저장 Wi-Fi 전체를 그대로 유지
- Enterprise에서 Personal로 전환할 때 이전 EAP 상태를 초기화해 인증 방식이 서로 간섭하지 않도록 처리
- Wi-Fi 연결 성공 이후의 DHCP·NTP·GitHub 업데이트 확인 상태기계는 1.5.11 동작을 그대로 유지
- 설정 포털에서 인증서 기반 EAP-TLS/사용자 지정 CA는 아직 지원하지 않음

## v1.5.11 업데이트 안내

- 펌웨어 버전을 1.5.11로 변경하고 기존 설정 스키마 6을 유지
- 업데이트 확인 상태 `U`가 동기식 HTTPS 요청 전에 실제 OLED에 먼저 렌더링되도록 보장
- 설정 포털 업데이트 설치 버튼의 브라우저 `confirm()` 의존을 제거하고 2단계 확인 방식으로 변경
- 설치 가능 여부와 설치 대기 상태를 펌웨어 API와 포털 UI가 직접 동기화하도록 보강
- OTA 설치 요청 후 HTTP 202 응답이 브라우저에 전달될 시간을 확보한 뒤 다운로드를 시작하도록 수정
- 수동 업데이트 확인에서도 새 버전 발견 시 OLED 전체 알림을 표시하고 최신 버전이면 메인 화면을 유지
- GitHub HTTPS 음수 오류 코드를 서버 HTTP 거부와 구분해 transport/TLS 단계 오류로 정확히 기록
- 기존 1.5.10의 Wi-Fi·NTP·GitHub 재시도, 백오프, 연결 복구 및 모뎀 절전 안정화 로직을 유지

## v1.5.10 업데이트 안내

- 펌웨어 버전을 1.5.10으로 변경하고 기존 설정 스키마 6을 유지
- 저해상도 OLED에서도 혼동되지 않도록 NTP 동기화는 우상단 `T`, 업데이트 확인은 `U`로 표시
- GitHub manifest 확인을 최대 3회로 분리하고 DNS·TLS·408·429·5xx 등 일시적 오류에 짧은 증가형 백오프 적용
- 업데이트 확인 중 Wi-Fi가 끊기면 즉시 실패 확정하지 않고 15초 빠른 Wi-Fi 복구 주기를 기다린 뒤 남은 확인 횟수를 사용
- GitHub DNS 사전 조회를 완전히 제거해 HTTPS 직전의 중복 resolver 호출과 추가 블로킹·실패 지점을 제거
- HTTPS 연결·읽기·TLS handshake 제한시간과 최대 리디렉션 수를 명시하고 요청마다 새 연결을 사용해 이전 실패 연결 상태의 재사용을 방지
- manifest 본문을 HTTPClient 전송 디코더로 읽어 chunked 응답을 처리하고, 길이 불일치·빈 본문·완결되지 않은 JSON을 일시적 전송 실패로 판정
- 업데이트 확인 실패와 실제 OTA 설치 실패 기록을 분리해 manifest 조회 오류가 설치 실패 상태를 덮어쓰지 않도록 수정
- 일시적인 업데이트 확인 실패는 10분 후 재시도하고, 비정상 manifest나 실제 OTA 실패는 기존 6시간 재시도 정책 유지
- Wi-Fi DISCONNECTED reason code와 GOT_IP 시 IP·게이트웨이·DNS·RSSI를 시리얼 로그에 추가
- 항상 연결 모드에서는 NTP·업데이트 종료 뒤에도 ESP 모뎀 절전을 켜지 않고 무선부를 유지하며, `동기화 뒤 Wi-Fi 끄기`가 켜진 경우에만 기존처럼 Wi-Fi 자체를 OFF
- Wi-Fi/DHCP가 복구되지 않은 상태에서는 GitHub HTTPS 재시도 횟수를 소모하지 않도록 업데이트 상태기계 보강
- 부팅 후 첫 NTP가 시간 초과되면 현재 Wi-Fi/DHCP 연결을 유지한 채 15초 후 NTP만 빠르게 재시도하고, 첫 성공 이후의 주기 동기화 실패는 기존 사용자 재시도 설정을 유지
- 펌웨어 다운로드는 manifest의 크기와 HTTP Content-Length가 정확히 일치할 때만 OTA 쓰기를 시작해 잘못된 스트림 기록 방지
- 동기식 GitHub HTTPS 확인을 시작하기 전에 `CHECKING` 상태를 실제 OLED에 먼저 렌더링하도록 변경해 정상적인 첫 시도에서도 우상단 `U`가 반드시 표시되도록 수정
- 설정 포털과 기기 세부정보 화면에서도 업데이트 확인 중에는 우상단에 `U`를 표시하고, NTP 동기화 중에는 `T`를 우선 표시
- 설정 포털의 업데이트 설치 버튼에서 브라우저 `confirm()` 의존을 제거하고 `업데이트 설치` → `정말 설치`의 2단계 확인 방식으로 변경
- 설치 가능 여부를 release metadata까지 검증한 `update_install_ready` 상태로 판단하고, 비활성 버튼 스타일·설치 요청/진행 상태를 명확하게 표시
- 포털에서 설치 요청을 수락한 뒤 실제 OTA 다운로드 시작까지 1.5초를 확보해 HTTP 202 응답과 사용자 피드백이 브라우저에 전달될 시간을 보장
- 포털 상태 응답에 설치 대기 상태를 포함해 브라우저 버튼 상태가 펌웨어의 실제 설치 큐와 동기화되도록 보강
- GitHub 연결 실패 로그를 HTTP 응답 오류와 HTTPS 전송/TLS 단계 오류로 구분해 `connection refused` 같은 HTTPClient 음수 코드를 서버의 HTTP 거부로 오해하지 않도록 진단 문구 개선
- 자동 확인뿐 아니라 설정 포털에서 수동 확인한 경우에도 새 버전이 발견되면 OLED `UPDATE AVAILABLE` 전체 화면을 표시하고, 최신 버전인 경우에는 별도 전체 화면을 표시하지 않음

## v1.5.9 업데이트 안내

- 펌웨어 버전을 1.5.9로 변경하고 기존 설정 스키마 6을 유지
- 콜드 부팅 STA 연결에서도 설정 모드와 동일하게 DHCP·NTP·업데이트 확인이 끝날 때까지 Wi-Fi 절전을 해제
- Wi-Fi 드라이버 시작 직후 100ms 안정화 시간을 두고 연결을 시작
- 연결 판정을 SSID 연결 여부가 아니라 IP·게이트웨이·DNS 확보 후 750ms 유지로 강화
- 콜드 부팅 연결 제한시간을 20초에서 30초로 늘려 DHCP가 늦은 공유기도 수용
- 연결이 끊기거나 후보 네트워크 연결에 실패하면 15초 뒤 빠르게 다시 시도
- NTP 실패 재시도 주기와 업데이트 실패 알림 정책은 기존 설정을 그대로 유지

## v1.5.8 업데이트 안내

- 펌웨어 버전을 1.5.8로 변경하고 기존 설정 스키마 6을 유지
- 부팅 로고를 약 3초 뒤 끝내고 NTP가 늦어져도 일반 화면에서 동기화를 계속하도록 변경
- NTP 제한시간을 60초로 늘려 첫 서버 응답 실패 시 대체 서버를 실제로 시도할 시간을 확보
- NTP와 HTTPS 확인 중 Wi-Fi 절전을 일시 해제하고 완료 후 원래 절전 정책 복원
- NTP 실패 시 연결된 Wi-Fi를 끊지 않고 같은 연결에서 NTP만 다시 시도하도록 복구 경로 변경
- 제한시간 직후 늦게 도착한 정상 NTP 응답도 버리지 않고 즉시 시간·디데이에 반영
- 새 부팅에서 실제 NTP 성공과 외부 왕복 연결이 확인된 뒤에만 자동 업데이트 점검 시작
- 업데이트 확인 중 전체 화면 대신 일반 화면 우상단에 회전 상태 아이콘 표시
- 현재 버전이 최신일 때 전체 화면 안내를 제거하고, 확인 실패·새 버전 발견 때만 안내 표시

## v1.5.7 업데이트 안내

- 펌웨어 버전을 1.5.7로 변경하고 기존 설정 스키마 6을 유지
- 공식 Waveshare ESP32-S3-Zero 보드 프로필의 온보드 2MB PSRAM을 활성화해 빌드
- OTA 실패 후 mDNS와 Wi-Fi 절전 상태를 복구하고, OTA 진행 상태를 저장하지 못하면 설치를 안전하게 중단
- 숨김 SSID를 포함해 저장된 모든 Wi-Fi를 직접 연결 후보로 시도하도록 복구 경로 보강
- 설정·Wi-Fi·초기화·OTA 관련 NVS 쓰기 실패를 감지하고 가능한 경우 이전 설정으로 롤백
- 기기 세부정보와 설정 포털에서는 사용하지 않는 PSRAM 항목을 계속 숨김

## v1.5.6 업데이트 안내

- 펌웨어 버전을 1.5.6으로 변경하고 기존 설정 스키마 6을 유지
- 기기 세부정보와 포털에서 사용하지 않는 PSRAM 항목을 제거하고 작업 스택 정보로 교체
- 릴리스 스크립트가 Waveshare 보드·OTA 파티션·PSRAM 비활성화 설정으로 직접 컴파일하도록 변경
- OTA 설치 보류와 설정 포털 종료 뒤 Wi-Fi 절전 상태가 확실히 복원되도록 수정
- 설정 API의 필수 항목·숫자·선택 값과 범위를 엄격하게 검증
- 가동 시간을 64비트 ESP 타이머로 변경해 약 49.7일 뒤 초기화되는 문제 수정
- 포털 Wi-Fi 검색에 서버 측 제한시간과 연결 작업 전 검색 정리 추가
- 세션 쿠키를 부분 문자열이 아닌 정확한 이름·값으로 검증
- BIN과 manifest를 모두 검증한 뒤에만 릴리스 파일을 교체하도록 생성 절차 강화
- 야간 시간 판정, OLED 대비, RGB LED 출력의 불필요한 반복 작업 감소
- 날짜 표시를 `strftime`으로 통일해 경고 빌드의 잘림 경고 제거

## v1.5.5 업데이트 안내

- 펌웨어 버전을 1.5.5로 변경하고 기존 설정 스키마 6을 유지
- 설정 Wi-Fi와 기기 세부정보를 제외한 OLED 상태·안내 문구를 실제 글자 폭 기준으로 중앙 정렬
- 기기 세부정보의 2초 BOOT 수동 페이지 전환을 제거하고 안정적인 5초 자동 페이지 순환으로 복원
- BOOT GPIO 인터럽트와 ISR 시간 측정 경로를 제거하고 단순 폴링·30ms 디바운스로 롤백해 버튼 입력 시 크래시 가능 요소 축소
- 버튼 홀딩 중 전체 OLED 갱신을 250ms로 제한해 I2C 전송과 메인 루프 부하 완화
- 짧은 클릭의 화면 상태 NVS 기록을 1.5초 지연·병합하고 버튼 입력 및 OTA 처리와 겹치지 않도록 변경
- 버튼 홀딩 시간 문자열과 페이지 전환 로그의 반복적인 동적 문자열 할당 제거
- 부팅 직후 재부팅 원인·내부 힙·최대 연속 블록·PSRAM 상태를 시리얼에 기록하도록 진단 강화
- 이 펌웨어에서는 PSRAM을 사용하지 않으므로 Arduino IDE 안정성 권장값을 `Disabled`로 명시

## v1.5.4 업데이트 안내

- 펌웨어 버전을 1.5.4로 변경하고 기존 설정 스키마 6을 유지
- 기기 세부정보 화면의 3초 자동 페이지 전환 제거
- 기기 세부정보 화면에서 `BOOT` 버튼을 2초 이상 3초 미만 누른 뒤 놓으면 다음 페이지로 수동 전환
- 기기 세부정보 화면에 다시 진입하면 첫 페이지부터 표시
- 1초 미만의 일반 화면 전환과 3초 이상의 설정 Wi-Fi 진입 동작은 그대로 유지

## v1.5.3 업데이트 안내

- 펌웨어 버전을 1.5.3으로 변경하고 기존 설정 스키마 6을 유지
- OTA 다운로드 화면의 `DOWNLOADING` 제목을 실제 글자 폭 기준으로 중앙 정렬
- 진행률 표시를 `%` 글리프가 포함된 전체 문자 폰트로 변경
- `0%`부터 `100%`까지 숫자와 퍼센트 기호를 하나의 문자열로 정확히 중앙 정렬

## v1.5.2 업데이트 안내

- 펌웨어 버전을 1.5.2로 변경하고 기존 설정 스키마 6을 유지해 1.5.1 설정을 그대로 보존
- OTA가 실행되는 Arduino `loopTask` 스택을 16KB로 명시하고 HTTPS 요청 로그와 설정 포털에 최소 잔여 스택 표시 추가
- 포털의 설치 승인 응답을 약 500ms 동안 전송한 뒤 실제 다운로드를 시작해 요청 접수와 OTA 시작 경계를 안정화
- 다운로드 중 칩 온도가 80°C에 도달하거나 센서 오류가 발생하면 즉시 설치를 중단하도록 과열 정책 통일
- Wi-Fi 절전 상태에서 설치를 누르면 저장된 Wi-Fi에 다시 연결하고 최신 Release 정보를 재확인하도록 개선
- 설치 전 연결·온도·RAM·OTA 공간 조건이 일시적으로 맞지 않을 때 확인된 새 버전 정보를 유지해 재시도 가능
- 새 펌웨어 부팅 후 약 10초 안정성 확인을 거쳐 설치 성공을 확정하고 지원 환경에서 OTA 롤백 취소 표시 수행
- Wi-Fi 목록 개수를 마지막에 기록하고 NVS에서 읽은 제목·문구 길이를 재검증해 전원 차단·설정 손상 복구 강화
- Release 생성 시 소스보다 오래된 BIN과 BIN 내부 버전 불일치를 차단하고 manifest JSON 제어문자 처리 강화
- 설정 포털의 업데이트 단계·재연결 안내 문구 개선

## v1.5.1 업데이트 안내

- 펌웨어 버전을 1.5.1로 변경
- 부팅 로고를 약 3초 동안 유지하면서 네트워크 연결은 백그라운드에서 계속 진행
- 최신 펌웨어와 일치할 때 `UP TO DATE` 화면을 약 1초 표시
- 시스템·메모리·저장공간·네트워크·시간 및 OTA 정보를 표시하는 기기 세부정보 화면 추가
- 설정 포털에 기기 세부정보 기본 모드·순환 항목과 상세 시스템 상태 추가
- 설정 스키마를 6으로 올리고 1.5.0의 Wi-Fi·표시 설정·순환 순서를 보존해 자동 마이그레이션
- OTA 다운로드 버퍼를 제한된 함수 스택에서 전역 RAM으로 이동해 설치 시작 직후 재부팅될 수 있는 문제 수정
- 연속 다운로드 중 워치독을 양보하고 설치 단계와 직전 중단 지점을 NVS에 기록하도록 진단 강화
- OTA 시작 시 설정용 AP를 즉시 종료하지 않고 검증 완료 후 재부팅할 때까지 연결 유지

## v1.5.0 업데이트 안내

- GitHub 공개 Release 기반 인터넷 펌웨어 업데이트 추가
- 부팅할 때마다 확인하고 정상 확인 후 7일마다 자동 재확인
- 사용자가 BOOT 버튼 또는 설정 포털에서 승인한 경우에만 설치
- HTTPS 인증서, 예상 파일 크기, SHA-256, OTA 슬롯 용량을 설치 전에 검증
- 다운로드 중 연결 단절·과열·센서 오류 발생 시 기존 펌웨어를 유지하고 중단
- 색상만이 아니라 점멸 횟수와 속도로 구별되는 업데이트 LED 상태 추가
- 업데이트 manifest 자동 생성 스크립트와 Release 게시 절차 추가
- GitHub Release 리디렉션 대상 검증용 ISRG Root X1 인증서와 DNS·TLS·HTTP 오류 진단 추가
- 기존 설정 스키마, 저장된 Wi-Fi, 여섯 가지 화면을 그대로 유지
- 기기 세부정보 화면은 OTA 검증용 1.5.1에서 추가 예정
